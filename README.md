# lyapunov-certified-imitation-learning
Learn a lyapunove certificat for a neural network


```bash
export PYTORCH_JIT=0
```

Install `mpc-datagen` based on this git repo:
```bash
pip install git+https://github.com/ArgoJ/mpc-datagen.git
```
or 
```bash
git clone https://github.com/ArgoJ/mpc-datagen.git && cd mpc-datagen && pip install .
```